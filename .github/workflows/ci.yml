name: CI

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 6 * * 1"  # weekly: surface ecosystem drift on quiet repos

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: gojiplus/py-canon/.github/workflows/reusable-ci.yml@v1
    with:
      wheel-import: devclean
      coverage-floor: 65
      # macOS-only tool: scanners shell out to BSD du/find and probe macOS paths.
      os: '["macos-latest"]'
      python-versions: '["3.11", "3.12", "3.13", "3.14"]'
